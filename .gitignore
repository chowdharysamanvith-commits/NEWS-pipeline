# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/

# Environment / secrets — NEVER commit these
.env
.env.*
*.secret

# Playwright
playwright/.local-browsers/

# Extraction output (large, committed via Actions only)
extraction_output/*.json

# Keep the directory itself
!extraction_output/.gitkeep

# Archive snapshots — committed via Actions
# (allow data/ to be tracked)

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
